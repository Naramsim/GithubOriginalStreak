{
  "name": "github-original-streak",
  "version": "0.9.0",
  "description": "Get back longest and current streak on Github prifiles",
  "scripts": {
    "test": "eslint .",
    "prebuild": "npm run lint && rimraf chrome/src/ firefox/data/ build",
    "build": "webpack",
    "build:watch": "webpack --watch",
    "package": " mkdir -p build && cd ./firefox && jpm xpi && mv *.xpi ../build && cd .. && zip -r ./build/chrome.zip ./chrome && cd ./build && cp chrome.zip chrome.nex"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Naramsim/GithubOriginalStreak.git"
  },
  "keywords": [
    "github",
    "streak",
    "original"
  ],
  "author": "Naramsim",
  "license": "MPL-2.0",
  "bugs": {
    "url": "https://github.com/Naramsim/GithubOriginalStreak/issues"
  },
  "homepage": "https://github.com/Naramsim/GithubOriginalStreak#readme",
  "devDependencies": {
    "babel-core": "^6.9.1",
    "babel-loader": "^6.2.4",
    "babel-preset-es2015": "^6.9.0",
    "css-loader": "^0.23.1",
    "eslint": "^2.12.0",
    "jpm": "^1.0.7",
    "rimraf": "^2.5.2",
    "style-loader": "^0.13.1",
    "webpack": "^1.13.1"
  }
}
